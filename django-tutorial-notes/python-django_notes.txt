### Step 1:install virtualenv
pip install virtualenv

### Step 2: Create a virtualenv
python -m virtualenv venv

### Step 3: activate virtualenv
<PATH>\venv\Scripts>activate
(venv) <PATH>\venv\Scripts>

### Step 4: deactivate virtualenv
(venv) <PATH>\venv\Scripts>deactivate

### Step 5 : lib list
<PATH>\venv\Scripts>activate
(venv) <PATH>\venv\Scripts>pip list

### Step 6 : install django
(venv) <PATH>\venv\Scripts>pip install django

### Step 7: lib list
(venv) <PATH>\venv\Scripts>pip list

### Step 8 : create admin project
(venv) <PATH>>django-admin startproject crm

### setup interpreter path in vscode
Shift+Ctrl+p > change the virtualenv interpreter path

### Runserver
python manage.py runserver

### create app
django-admin startapp webapp

### update setting.py 
INSTALLED_APPS = [
   
    "webapp",
]


###  migrate
python manage.py migrate

### createsuperuser
python manage.py createsuperuser
http://127.0.0.1:8000/admin
django/Seagate@123

### crm > urls.py update
create a file urls.py file under webapp

goto crm > urls.py update with below

from django.contrib import admin
from django.urls import path,include

urlpatterns = [
    path("admin/", admin.site.urls),

    path('',include('webapp.urls')),
]

### crm > webapp > urls.py update with below

from django.urls import path
from . import views

urlpatterns = [
    path('',views.home,name="")
]

### crm > webapp > view.py update with below

from django.shortcuts import render
from django.http import HttpResponse

def home(request):
    return HttpResponse("Hello world")